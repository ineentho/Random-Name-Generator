==RANDOM NAME GENERATOR==
Random name generator is a plugin for Google Chrome that can generate completley random names,
names from a name list or a combination of these two. It is a full rewrite on the current version that
currently is avaliable at the Chrome Webstore.